% model = multigpCreate(q, d, X, y, options);

% options = multigpOptions(approx);

% [params, names] = multigpExtractParam(model);

% model = multigpExpandParam(model, params);

% [Y, fPos] = multigpOut(model, x, f, varargin);